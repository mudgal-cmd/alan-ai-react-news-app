import { styled } from "@mui/material/styles";
// Below is the way to style material ui components.
const useStyles =  styled ("container")({
  container: {
    padding: "0 5%",
    width: "100%",
    margin : "0"
  }
})

export default useStyles;
